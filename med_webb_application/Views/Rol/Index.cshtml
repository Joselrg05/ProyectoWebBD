@model List<med_webb_CapaDato.Modelado.Role>

@{
    ViewBag.Title = "Roles";
    string descripcion = "Listado de Roles";
    int i = 1;
}

<br>
<div class="row">
    <div class="col-md-12">
        <div class="panel panel-primary">
            <div class="panel-heading">
                <h4 class="text-center"><strong>@descripcion</strong></h4>
            </div>
            <div class="panel-body">
                <div class="row">
                    <div class="col-md-12">
                        @Html.ActionLink("+ Agregar", "Crear", "Rol", null, new { @class = "btn btn-primary" })
                    </div>
                </div>
                <hr>
                <table class="table table-bordered">
                    <thead>
                        <tr class="success">
                            <th class="text-center">#</th>
                            <th class="text-center">Nombre</th>
                            <th class="text-center">Permisos</th>
                            <th class="text-center">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="text-center"><strong>@i</strong></td>
                                <td>@item.Nombre</td>
                                <td>@item.Permisos</td>
                                <td class="text-center">
                                    <div class="btn-group">
                                        @Html.ActionLink(" ", "Editar", "Role", new { id = item.Id }, new { @class = "btn btn-success glyphicon glyphicon-edit" })
                                        @Html.ActionLink(" ", "Eliminar", "Role", new { id = item.Id }, new { @class = "btn btn-danger glyphicon glyphicon-trash" })
                                    </div>
                                </td>
                            </tr>
                            i++;
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
